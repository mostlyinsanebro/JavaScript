'use strict';

// In order to select an element from the html page, we can use the selector
// querySelector function to select the element using its .element or #id.
// textContent is used to select the text content that is there in the tag.
console.log(document.querySelector('.message').textContent);
