/*
if else is used to control the execution of our code. We can make some of code execute and 
 some of our code not execute using if else.
 if else takes the conditions as inputs and these consitions return boolean as result
 true or flaese, so if else takes boolean as input.
 else is optional in if else.
*/

const age = 15;

if(age >= 18)
{
   console.log("Sarah can have her driving license :)")
}
else
{
   console.log(`Sarah is too young. She will have to wait for another ${18 - age} years.`);
}



const year = 2023;
let century;
if(year >= 2000)
{
    century = 21;
}
else
{
    century = 21;
}
console.log(century);
